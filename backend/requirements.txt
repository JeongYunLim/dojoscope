# 버전을 ">="로만 열어두면 pip install 시점마다 그때 최신 버전이 깔려서,
# 로컬/도커/서버가 서로 다른 시점에 빌드되면 numpy/scikit-learn/umap-learn의
# 내부 구현 차이 때문에 seed(42)·n_jobs=1을 고정해도 UMAP 좌표가 달라질 수
# 있다(실제로 이 문제가 재현돼서 정확히 이 원인으로 확인함). "=="로 고정해
# 어떤 환경에서 빌드하든 항상 같은 버전을 쓰게 한다.
# (로컬 PC는 Python 3.14라 더 최신 numpy(2.4.4 등)가 깔려 있지만, 그 버전은
# 도커 이미지의 Python 3.10용 wheel이 없어서(pip download로 직접 확인) 여기서는
# Python 3.10과 호환되는 조합으로 고정했다 — 로컬과 도커가 100% 동일 버전은
# 아니지만, 적어도 도커 빌드끼리는(서버 재배포해도) 항상 같은 결과가 나온다.)
numpy==1.26.4
scipy==1.11.4
scikit-learn==1.4.2
umap-learn==0.5.6
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
