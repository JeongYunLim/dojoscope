# 버전을 ">="로만 열어두면 pip install 시점마다 그때 최신 버전이 깔려서,
# 로컬/도커/서버가 서로 다른 시점에 빌드되면 numpy/scikit-learn/umap-learn의
# 내부 구현 차이 때문에 seed(42)·n_jobs=1을 고정해도 UMAP·군집 결과가 달라질
# 수 있다(실제로 이 문제가 재현돼서 정확히 이 원인으로 확인함).
# "=="로 로컬 개발 환경에 설치된 버전과 정확히 똑같이 고정했다 — 즉 지금까지
# 이 프로젝트에서 확인해온 UMAP/군집 결과가 서버(Docker)에서도 그대로 나온다.
# Dockerfile의 베이스 이미지를 Python 3.14로 맞춘 것도 이 버전들의 wheel이
# 그 Python 버전에서만 존재하기 때문(직접 확인).
numpy==2.4.4
scipy==1.18.1
scikit-learn==1.9.0
umap-learn==0.5.12
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
