# 진짜 의미 임베딩(bge-small-en-v1.5)을 쓰고 싶을 때만 추가로 설치.
# torch가 함께 설치되어 용량이 크고(수백 MB), 최초 실행 시
# huggingface.co에서 모델 가중치를 내려받으므로 인터넷 연결이 필요하다.
#
#   pip install -r requirements.txt -r requirements-embedding.txt
#
# 이게 없으면 pipeline/vectorize.py의 Embedder가 자동으로
# 해싱 기반 폴백 임베딩으로 전환되어 파이프라인은 계속 동작한다
# (품질은 떨어지지만 구조/로직 검증에는 충분).

sentence-transformers>=2.7
