# 파이프라인 실행에 필요한 핵심 의존성.
# sentence-transformers가 없어도(=인터넷이 안 되거나 설치를 안 해도)
# pipeline/vectorize.py의 Embedder가 TF-IDF+SVD(LSA) 폴백으로 자동 전환되어
# 아래 패키지만으로 전체 파이프라인이 끝까지 동작한다.
#
# 서버(Flask)는 쓰지 않는다 — 화면(viz/)은 정적 파일만 읽고, 데이터 추가는
# add_data.py 스크립트가 그 파일들을 다시 만드는 방식이라 별도 웹 서버가 필요 없다.

numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
umap-learn>=0.5
matplotlib>=3.8
